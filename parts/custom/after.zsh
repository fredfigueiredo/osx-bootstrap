# Set prompt
prompt skwpcustom

# Define your own aliases or override those provided by YADR
alias hosts='sudo emacs /private/etc/hosts'

# Set emacs has default editor
export EDITOR=emacs

# Add emacs-like key bindings to shell
# Missing after YADR installation
bindkey -e
